{
  "name": "@transcend-io/developer-tools-monorepo",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint lint:root",
    "lint:fix": "turbo run lint:fix lint:fix:root",
    "typecheck": "turbo run typecheck",
    "format": "turbo run format format:root",
    "format:check": "turbo run format:check format:check:root",
    "quality": "turbo run quality",
    "quality:fix": "turbo run quality:fix",
    "changeset": "changeset",
    "check:changeset": "node ./scripts/check-changeset.mjs",
    "version-packages": "changeset version",
    "release": "turbo run build && changeset publish",
    "check-exports": "turbo run check-exports",
    "syncpack:lint": "turbo run syncpack:lint syncpack:lint:root",
    "lint:root": "oxlint .",
    "lint:fix:root": "oxlint --fix .",
    "format:root": "oxfmt --write .",
    "format:check:root": "oxfmt --check .",
    "syncpack:lint:root": "syncpack lint"
  },
  "devDependencies": {
    "@changesets/cli": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "syncpack": "catalog:",
    "turbo": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@10.24.0"
}
