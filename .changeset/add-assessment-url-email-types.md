---
"@transcend-io/privacy-types": patch
"@transcend-io/cli": patch
---

Add `URL` and `EMAIL` variants to `AssessmentQuestionType` for assessment form questions, and regenerate `transcend.yml` JSON schema.
