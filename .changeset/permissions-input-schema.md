---
'@transcend-io/cli': patch
---

Align the Permissions `policy new` starter input schema and purpose slugs with the live Permissions API OPA contract (`preferences` + `context`, Preference Store-style names like `Analytics`).
