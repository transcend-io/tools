---
'@transcend-io/mcp-app-ui': minor
'@transcend-io/mcp-server-consent': patch
---

Add `@transcend-io/mcp-app-ui` for shared presentational MCP App components (Button, Card, StatusCard, ProgressBar, and named icons such as `CheckIcon` / `TrashIcon` over real `.svg` files via SVGR `?react`), and use Button/Card/StatusCard/ProgressBar from cookie-triage.
