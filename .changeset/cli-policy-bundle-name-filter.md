---
'@transcend-io/cli': patch
---

Resolve Policy Engine bundles and versions by name via list filters instead of paginating full listings. Look up versions by UUID on the nested bundle route used by download (not the flat policy-bundle-versions path).
