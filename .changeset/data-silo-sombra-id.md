---
"@transcend-io/cli": minor
"@transcend-io/sdk": minor
---

Add optional `sombra-id` on data silos in `transcend.yml` so inventory pull/push can sync the GraphQL `DataSilo.sombraId` association for multi-Sombra organizations.
