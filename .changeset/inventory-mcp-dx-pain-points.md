---
'@transcend-io/mcp-server-inventory': minor
'@transcend-io/mcp-server-base': patch
'@transcend-io/mcp': minor
---

Improve inventory MCP DX: split data-silo create into catalog `integrationName` + optional display `title`/`description`, add `text` (and silo `titles`) list filters, and stop fabricating datapoint timestamps.
