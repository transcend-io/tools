---
'@transcend-io/privacy-types': minor
---

Add `DsrErrorCode.DropRunNotIntakeEligible` with its `DSR_ERROR_MESSAGE` builder, emitted when a bulk DSR submission references a DROP run whose state no longer accepts intake.
