---
'@transcend-io/mcp-server-base': patch
---

Address MCP app configuration review follow-ups: hoist the client capability report in `buildMcpServer`, and name the HTML doctype check used by UI resources.
