---
"@transcend-io/privacy-types": minor
---

Split `LoadOptions.themeConfigMap` so it accepts only `ThemeConfigurationMinimal` values, and add `MobileUiLoadOptions` with `themeConfigMap` mapped to full `ThemeConfiguration` values.
