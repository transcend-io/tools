---
'@transcend-io/privacy-types': patch
---

Scope `DhContextRequired` and `ConcurrentSubmissionConflict` under `DsrBulkErrorCode` (no single `input[]` index can be attributed) and restore `DropIdentifierCoverageMismatch` and `DuplicateDropRecords` message wording from main.
