---
"@transcend-io/cli": minor
---

Document the optional `partition` object on cron pending-requests responses and include `partitionId`, `partitionName`, and `partitionKey` columns in `transcend request cron pull-identifiers` CSV output when a DSR is scoped to a consent partition.
