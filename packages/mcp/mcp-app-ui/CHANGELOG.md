# @transcend-io/mcp-app-ui

## 0.1.0

### Minor Changes

- Initial release: presentational Button, Card, StatusCard, ProgressBar, and named icons (`CheckIcon`, `QuestionCircleIcon`, `RightCaretIcon`, `TrashIcon`) over real `.svg` files via SVGR `?react` for MCP App views.
