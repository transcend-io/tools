import { createSvgIcon } from '@transcend-io/mcp-ui-common';

import commentSvg from './comment.svg';

/** Speech-bubble icon used by the cookie triage notes control. */
export const CommentIcon = createSvgIcon(commentSvg, 'CommentIcon');
