import { createSvgIcon } from '@transcend-io/mcp-ui-common';

import cancelSvg from './cancel.svg';

/** Close / X used by the cookie triage junk control. */
export const CancelIcon = createSvgIcon(cancelSvg, 'CancelIcon');
