import { createSvgIcon } from '@transcend-io/mcp-ui-common';

import trashSvg from './trash.svg';

/** Trash icon used by the cookie triage delete control. */
export const TrashIcon = createSvgIcon(trashSvg, 'TrashIcon');
