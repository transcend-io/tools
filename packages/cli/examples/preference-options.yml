# Standalone preference option values (Preferences -> Preference Topics -> Options tab).
# Slugs use ConstantCase. Reference these from preference-topics[].options by slug.
preference-options:
  - title: Email notifications
    slug: EmailNotifications
  - title: SMS alerts
    slug: SmsAlerts
  - title: Push notifications
    slug: PushNotifications
